import React, { Component } from "react";
import { Grid, Menu } from "../components/index";

class Main extends Component {
  render() {
    return (
      <div>
        <Menu />
        <Grid />
      </div>
    );
  }
}

export default Main;
